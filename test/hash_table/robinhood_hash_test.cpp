// TODO: add include
#include ""

#include <bits/stdc++.h>
#include "gtest/gtest.h"

TEST(RobinhoodHash, basic) {
    // TODO: declare a robinhood hash table
    t.put(1, 2);
    EXPECT_EQ(t.get(1), 2);
    t.put(1, 3);
    EXPECT_EQ(t.get(1), 3);
}
